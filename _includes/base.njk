<!DOCTYPE html>
<html lang="en-AU">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <title>{{ title }} | {{ site.business_name }}</title>
  <meta name="description" content="{{ description | default(site.business_name ~ ' - Local taxi service in ' ~ site.city_region) }}">
  
  <!-- Open Graph -->
  <meta property="og:title" content="{{ title }} | {{ site.business_name }}">
  <meta property="og:description" content="{{ description | default(site.business_name ~ ' - Local taxi service in ' ~ site.city_region) }}">
  <meta property="og:image" content="{{ site.base_url }}/assets/images/{{ seo_image | default('og-image.webp') }}">
  <meta property="og:url" content="{{ site.base_url }}{{ page.url }}">
  <meta property="og:type" content="website">
  <meta property="og:locale" content="en_AU">
  
  <!-- Favicon -->
  <link rel="icon" href="/assets/images/favicon.svg" type="image/svg+xml">
  
  <!-- Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  
  <!-- Styles -->
  <link rel="stylesheet" href="/assets/css/styles.css">
  
  <!-- JSON-LD Structured Data -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "TaxiService",
    "name": "{{ site.business_name }}",
    "description": "Local taxi service in {{ site.service_areas }}",
    "url": "{{ site.base_url }}",
    "telephone": "{{ site.phone_e164 }}",
    "areaServed": ["Kyneton", "Heathcote", "Mia Mia", "Redesdale"],
    "provider": {
      "@type": "Person",
      "name": "{{ site.driver_name }}"
    },
    "address": {
      "@type": "PostalAddress",
      "addressLocality": "Kyneton",
      "addressRegion": "Victoria",
      "addressCountry": "AU"
    }
  }
  </script>
</head>
<body>
  <a href="#main-content" class="skip-link">Skip to main content</a>
  
  {% include "header.njk" %}
  
  <main id="main-content" class="main">
    {{ content | safe }}
  </main>
  
  {% include "footer.njk" %}
</body>
</html>
